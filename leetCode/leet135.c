#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int candy(int* ratings, int ratingsSize);

int main() {
    int ratings[] = {1,2,2};
    int ratingsSize = 3;

    int sugar = candy(ratings, ratingsSize);

    printf("%d", sugar);

    return 0;
}

int candy(int* ratings, int ratingsSize) {
    
    int count = ratingsSize;
    int l = 0, i = 0;

    while (i < ratingsSize){
        
        
        i++;
    }
    
    

    return count + l;
      
}